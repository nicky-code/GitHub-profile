export class Repository {

    // tslint:disable-next-line: variable-name
    constructor(public full_name: string, public description: string, public created_at: any, public updated_at: any) {
    }
}
